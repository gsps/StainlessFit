// NOTE: [|0|] is ignored when run with --partial, just there to also compile without it

val result =
  fix[z => Nat => Nat](fac =>
    fun of (n [Nat]) = {
      if (n > 0) { n * fac (n-1) } else { 1 }
    }
  ) [|0|] 6;

result
