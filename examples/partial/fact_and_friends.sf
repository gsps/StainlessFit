val fac =
  fix[z => Nat => Nat](fac =>
    fun of (n [Nat]) = {
      if (n > 0) { n * fac (n-1) } else { 1 }
    }
  ) [|0|];

val result = match (right (fac 3)) {
  case left(x) => 0
  case right(n) => n
};

result
